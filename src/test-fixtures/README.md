# Test fixtures

`anatomy-v4-before-transitions.glb` is the pre-transition anatomy asset used to verify that face edits preserve the original protected vertices and topology. It retains the Z-Anatomy / BodyParts3D attributions and applicable share-alike terms in [NOTICE](../../public/models/NOTICE.txt).

`legacy-anatomyAsset.ts` is the previous skin-weight implementation, kept only for `scripts/audit-skin-deformation.ts --legacy-skin`. The application does not import it.
